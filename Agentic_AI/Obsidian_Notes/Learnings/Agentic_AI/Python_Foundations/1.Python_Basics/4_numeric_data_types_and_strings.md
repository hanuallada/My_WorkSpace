![[Pasted image 20260907192515.png]]

==**String Data types**==

![[Pasted image 20260907192803.png]]

==**Indexing and Slicing**==

indexing for a given string starts from zero
![[Pasted image 20260907193155.png]]

If we refer to the index position which is greater than the length of the string then we will get ==**index array out of range error**==

**==String Concatenation==**

![[Pasted image 20260907193730.png]]

==**Common String Functions**==

- len(): Returns length of a string
- lower(), upper(): converts a string to lower or upper case
- strip(): removes leading and trailing spaces
- replace(): Replaces a substring with another substring
- split(): Splits a string into list of substrings based on delimiter
- find(): Index(): Finds the index of substring in the string

![[Pasted image 20260907194606.png]]